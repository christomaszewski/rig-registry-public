# AUTO-UPDATED on every sbg_driver release by that repo's registry-release workflow
# (rewrites this file wholesale + regenerates index.json; name comes from rigging.yaml
# `service:`). Hand-edits will be clobbered by the next release — change rigging.yaml
# or the workflow in the sbg_driver repo instead.
kind: service
name: sbg
version: 0.3.0
platforms: [linux/arm64]
ros_distro: lyrical
instantiable: true            # template service — a profile (or hand-authored config) instantiates it
source:
  repo: https://github.com/christomaszewski/sbg_driver.git
  rev: def13aaf7836a69662345bfa097f4b431f6ed478
